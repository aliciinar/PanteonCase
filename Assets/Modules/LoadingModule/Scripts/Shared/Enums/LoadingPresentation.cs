namespace Modules.LoadingModule.Shared.Enums
{
    /// <summary>What the player sees while a set runs. Silent shows nothing and still announces.</summary>
    public enum LoadingPresentation
    {
        Silent = 0,
        Fullscreen = 1,
        Overlay = 2
    }
}
